# Bundled Sources

The PDF in this folder is intentionally shipped with the project so local and static deployments have a real source file available:

- `life-after-treatment.pdf` — National Cancer Institute patient education resource.

The chat also uses curated bilingual source records from `server/medicalKnowledge.ts` and links to the official source pages in each answer.
